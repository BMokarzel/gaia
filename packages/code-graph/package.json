{
  "name": "@topology/code-graph",
  "version": "0.1.0",
  "description": "Language-agnostic AST → Element/Edge graph + FlowTree builder. Autonomous from @topology/core.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "code-graph": "dist/cli/index.js"
  },
  "files": [
    "dist",
    "schema"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "tree-sitter": "^0.21.1",
    "tree-sitter-typescript": "^0.21.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
